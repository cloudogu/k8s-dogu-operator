---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: authregistrations.k8s.cloudogu.com
  labels:
    app: ces
    app.kubernetes.io/name: k8s-auth-registration-lib
spec:
  group: k8s.cloudogu.com
  names:
    kind: AuthRegistration
    listKind: AuthRegistrationList
    plural: authregistrations
    shortNames:
      - ar
    singular: authregistration
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Requested auth protocol
          jsonPath: .spec.protocol
          name: Protocol
          type: string
        - description: Registered consumer/service name
          jsonPath: .spec.consumer
          name: Consumer
          type: string
        - description: Effective Secret used for credentials
          jsonPath: .status.resolvedSecretRef
          name: Secret
          type: string
        - description: Whether credentials were published to a Secret
          jsonPath: .status.conditions[?(@.type == 'CredentialsPublished')].status
          name: CredsPublished
          type: string
        - description: Whether the registration has been completed
          jsonPath: .status.conditions[?(@.type == 'Completed')].status
          name: Completed
          type: string
        - description: The age of the resource
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1
      schema:
        openAPIV3Schema:
          description: AuthRegistration is the Schema for the authregistrations API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of AuthRegistration
              properties:
                consumer:
                  description: Consumer is the identifier of the consuming application (service name).
                  minLength: 1
                  type: string
                logoutURL:
                  description: LogoutURL defines an optional logout URL for single logout integrations.
                  format: uri
                  type: string
                params:
                  additionalProperties:
                    type: string
                  description: Params contains additional optional protocol-specific parameters.
                  type: object
                protocol:
                  description: |-
                    Protocol defines which authentication protocol should be used for registration.
                    Supported values are CAS, OIDC and OAUTH.
                  enum:
                    - CAS
                    - OIDC
                    - OAUTH
                  type: string
                secretRef:
                  description: |-
                    SecretRef references an optional Secret name where generated credentials should be stored.
                    If this field is omitted, the controller creates and manages a Secret automatically.
                  maxLength: 253
                  minLength: 1
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                  type: string
              required:
                - consumer
                - protocol
              type: object
            status:
              description: status defines the observed state of AuthRegistration
              properties:
                conditions:
                  description: |-
                    conditions represent the current state of the AuthRegistration resource.
                    Each condition has a unique type and reflects a specific state transition.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                resolvedSecretRef:
                  description: |-
                    ResolvedSecretRef is the effective Secret name used to store credentials.
                    It points either to spec.secretRef or to a controller-generated Secret when spec.secretRef is not set.
                  type: string
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}